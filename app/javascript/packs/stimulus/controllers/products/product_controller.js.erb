import { Controller } from "stimulus"

export default class extends Controller {
	static targets = ["display", "input"]

	updateDisplay() {
		this.displayTarget.innerText = (this.inputTarget.value * this.data.get("price")).toFixed(2) + "  €"
	}
}
