<%= simple_form_for(@recipe) do |f| %>
	<%= f.input :name %>
	<%= f.input :description %>
	<%= f.rich_text_area :information %>
	<%= f.input :public %>
	<%= f.file_field :images, multiple: true %>	

	<%= f.submit %>
<% end %>