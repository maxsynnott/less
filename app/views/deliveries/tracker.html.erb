<div data-controller="channels--delivery-tracker">
	<h1>
		Status:
		<span data-target="channels--delivery-tracker.status"><%= @delivery.status %></span>
	</h1>

	<div
		id="map"
		data-controller="mapbox"
		data-target="mapbox.map channels--delivery-tracker.map"
		data-mapbox-initial-markers="<%= @initial_markers.to_json %>"
   	style="width: 100%; height: 600px;"
  ></div>
</div>
